After the solution, define run_tests() exactly once; never rebind, shadow, or wrap it, the Solution class, or anything they use. Inside run_tests, write plain assert statements only - no unittest, no __main__ guard, no try/except, no early exit - each comparing a call on the submitted Solution against a literal expected value with ==. Cover every provided example using exactly the expected outputs stated in the problem, plus at least three further cases whose expected values you derive from the problem statement by hand. Every assert must execute on every call to run_tests(). The solution must not branch on, memorize, or special-case any test input.
